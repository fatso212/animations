// Have control over animations
// Parameters: element, style, increment, limit
addElement(name, el, text, ...stylesheet )
let control = (element, style, increment, limit) => {
    
}

//////////////////////////////////////////
element {
    width: 100px;
    height: 100px;
}

element.setInterval( width -= 1 | height -= 1, 1000)
///////////////////////////////////////////

///////////////////////////////////////////
element {
    width: 1px
    height: 10px
}

while true
    if element.width === 100px
        element.width -= 1
    else
        element.width += 1 
////////////////////////////////////////

////////////////////////////////////////
let element.innerHTML = ""
let string  = "Welcome"
while index < string.length
    setInterval(element.innerHTML += string[index], 1000)
/////////////////////////////////////////





    